# Карта архитектуры KSRF-скиллов в нотации C4

> Черновик для обсуждения. Файл создан локально и пока не предназначен для коммита.

Эта карта показывает один и тот же набор скиллов на нескольких уровнях абстракции. Поскольку KSRF-скиллы являются пакетами инструкций, справочников и скриптов, а не отдельными сетевыми сервисами, уровень контейнеров ниже используется как **логическое разбиение на функциональные подсистемы**.

| Представление | На какой вопрос отвечает |
| --- | --- |
| Уровень 1 — System Context | кто использует набор и с какими внешними источниками он работает |
| Уровень 2 — Containers | на какие крупные функциональные подсистемы распадается полный цикл |
| Уровень 3 — Components | как связаны все 12 KSRF-скиллов |
| Dynamic | как проходит типовой маршрут подготовки жалобы |
| Implementation и Deployment | из чего состоит пакет скилла и как skillset публикуется и исполняется |

## Уровень 1. System Context

```mermaid
C4Context
title KSRF Skillset — системный контекст

Person(applicant, "Заявитель", "Самостоятельно исследует дело, предоставляет документы и принимает решение об обращении")
Person(lawyer, "Юрист", "Проверяет источники, выбирает правовую позицию и дорабатывает документы")

System_Ext(agent, "Агентная среда", "Подключает скиллы, ведёт диалог и создаёт рабочие артефакты")
System(skillset, "KSRF Skillset", "Проводит дело от первичной оценки до подачи и исполнения решения")
SystemDb_Ext(casefiles, "Материалы конкретного дела", "Судебные акты, жалобы, приложения, даты и подтверждающие документы")
SystemDb_Ext(official, "Официальные правовые источники", "КС РФ, действующее законодательство, официальная судебная практика и HUDOC")

Rel_R(applicant, agent, "Передаёт материалы; получает маршрут")
Rel_R(lawyer, agent, "Проверяет и дорабатывает")
Rel_R(agent, skillset, "Подключает и исполняет")
Rel_D(skillset, casefiles, "Читает материалы дела")
Rel_D(skillset, official, "Проверяет право и практику")

UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

### Что видно на этом уровне

- skillset не заменяет заявителя или юриста, а организует и усиливает их работу;
- материалы дела и официальные источники остаются внешними источниками истины;
- агентная среда отвечает за диалог и исполнение, а KSRF Skillset — за специализированную методологию.

## Уровень 2. Containers — функциональные подсистемы

```mermaid
C4Container
title KSRF Skillset — логические подсистемы полного цикла

Person(user, "Заявитель или юрист", "Работает с делом самостоятельно или совместно со специалистом")
System_Ext(agent, "Агентная среда", "Исполняет инструкции и сохраняет рабочие результаты")
SystemDb_Ext(casefiles, "Материалы дела", "Документы, даты, судебные акты и приложения")
SystemDb_Ext(official, "Официальные источники", "Законодательство и судебная практика")

System_Boundary(skillset, "KSRF Skillset") {
  Container(cycle, "Оркестратор полного цикла", "ksrf-complaint-cycle", "Выбирает маршрут, запускает этапы и контролирует переходы")
  Container(route, "Маршрутизация и допустимость", "triage + exhaustion + court request", "Проверяет hard gates, срок, исчерпание и альтернативный запрос суда")
  Container(research, "Адаптивное исследование", "explore + patterns + ECHR", "Строит конкурирующие гипотезы, контраргументы и сравнительный слой")
  Container(drafting, "Факты, требования и правовая позиция", "facts-demands + rights-builder", "Связывает норму, применение, вред, аргументы и способы защиты")
  Container(assurance, "Независимая проверка", "complaint-qa + formal-filing-check", "Моделирует отказ и проверяет готовность проекта и пакета")
  Container(outcome, "Альтернативный маршрут и исполнение", "court-request-motion + decision-execution", "Поддерживает запрос суда и действия после акта КС РФ")
  ContainerDb(knowledge, "Встроенная исследовательская база", "Markdown + JSON", "Методология, паттерны, графы, карты доказательств и официальные ориентиры")
  ContainerDb(artifacts, "Рабочие артефакты", "CaseFile + ArgumentPortfolio + drafts", "Проверяемые промежуточные результаты между этапами")
}

Rel(user, agent, "Ставит задачу, подтверждает факты и принимает решения")
Rel(agent, cycle, "Запускает основной или альтернативный маршрут")
Rel(cycle, route, "Проверяет возможность и своевременность обращения")
Rel(cycle, research, "Запускает исследование после прохождения hard gates")
Rel(cycle, drafting, "Передаёт утверждённый портфель гипотез")
Rel(cycle, assurance, "Передаёт проект на независимую проверку")
Rel(cycle, outcome, "Подключает специальный маршрут или исполнение")
Rel(route, casefiles, "Читает процессуальную историю")
Rel(research, official, "Проверяет первичные правовые источники")
Rel(research, knowledge, "Получает гипотезы, аналоги и контрпримеры")
Rel(drafting, artifacts, "Создаёт проверяемые разделы жалобы")
Rel(assurance, artifacts, "Проверяет трассировку и комплектность")
Rel(cycle, artifacts, "Управляет состоянием работы")

UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="2")
```

### Ключевая архитектурная граница

Процесс разделён на две части:

1. **Жёсткая процессуальная оболочка** — допустимость, применение нормы, конкретное дело, исчерпание, срок и источники.
2. **Адаптивное исследование** — конкурирующие объяснения дефекта нормы, альтернативные тесты, контраргументы и способы устранения проблемы.

Сильная содержательная гипотеза не компенсирует провал обязательного процессуального условия.

## Уровень 3. Components — карта 12 скиллов

```mermaid
C4Component
title KSRF Skillset — взаимодействие скиллов

Container(agent, "Агентная среда", "Codex / совместимый агент", "Подключает скиллы и ведёт рабочую сессию")
SystemDb_Ext(casefiles, "Материалы дела", "Факты, документы и судебная история")
SystemDb_Ext(official, "Официальные источники", "Нормы и судебная практика")
ContainerDb_Ext(corpus, "Встроенный корпус", "Markdown + JSON", "Паттерны, карты, графы и исследовательские справочники")

Container_Boundary(skillset, "KSRF Skillset") {
  Component(cycle, "Полный цикл жалобы", "ksrf-complaint-cycle", "Оркестрирует маршрут и переходы между этапами")
  Component(triage, "Первичная оценка", "ksrf-case-triage", "Отделяет hard gates от содержательных гипотез")
  Component(exhaustion, "Исчерпание и срок", "ksrf-exhaustion-planner", "Строит процессуальный маршрут и проверяет готовность")
  Component(motion, "Запрос обычного суда", "ksrf-court-request-motion", "Готовит альтернативный маршрут до завершения дела")
  Component(explore, "Исследование аргументов", "ksrf-explore-arguments", "Сравнивает principal, reserve, experimental и rejected hypotheses")
  Component(patterns, "Библиотека аргументативных паттернов", "ksrf-argument-patterns", "Даёт аналогии, различия, контрпримеры и формулы")
  Component(echr, "Сравнительный слой ЕСПЧ", "ksrf-echr-argumentation", "Проверяет позитивные обязательства, proportionality и effective remedy")
  Component(facts, "Факты и требования", "ksrf-complaint-facts-demands", "Связывает норму, применение, вред, вопрос и просительную часть")
  Component(rights, "Правовая позиция", "ksrf-rights-argument-builder", "Превращает утверждённый портфель в проверяемую аргументацию")
  Component(qa, "Проверка качества", "ksrf-complaint-qa", "Моделирует отказ и проверяет трассировку и устойчивость")
  Component(formal, "Формальная готовность", "ksrf-formal-filing-check", "Проверяет реквизиты, приложения, полномочия и канал подачи")
  Component(execution, "Исполнение решения", "ksrf-decision-execution", "Планирует пересмотр, компенсацию и нормативное исполнение")
}

Rel_D(agent, cycle, "Запускает")
Rel_D(cycle, triage, "Определяет готовность")
Rel_D(triage, exhaustion, "Передаёт CaseFile и hard gates")
Rel_R(exhaustion, motion, "Открывает альтернативный маршрут")
Rel_D(exhaustion, explore, "Разрешает исследование")
Rel_R(explore, patterns, "Запрашивает аналоги и контрпримеры")
Rel_L(explore, echr, "Запрашивает сравнительный слой")
Rel_D(explore, facts, "Передаёт ArgumentPortfolio")
Rel_D(facts, rights, "Передаёт норму, применение, вред и remedy")
Rel_D(rights, qa, "Передаёт проект аргументации")
Rel_D(qa, formal, "Разрешает формальную комплектацию")
Rel_D(formal, execution, "Передаёт post-decision контекст")
Rel_D(casefiles, triage, "Предоставляют факты")
Rel_D(official, exhaustion, "Подтверждают правила")
Rel_D(corpus, patterns, "Предоставляет паттерны и карты")

UpdateLayoutConfig($c4ShapeInRow="4", $c4BoundaryInRow="1")
```

Стрелки показывают доминирующий поток артефактов. Управление переходами на каждом этапе остаётся у `ksrf-complaint-cycle`.

### Каталог компонентов

| Скилл | Основная ответственность | Ключевой результат |
| --- | --- | --- |
| `ksrf-complaint-cycle` | оркестрация полного цикла | согласованный маршрут и состояние работы |
| `ksrf-case-triage` | первичная допустимость и готовность | решение: исследовать, укрепить процесс, запросить материалы или остановиться |
| `ksrf-exhaustion-planner` | исчерпание, применение нормы и срок | процессуальная линия и список следующих действий |
| `ksrf-court-request-motion` | запрос суда и ходатайство стороны | квалификация маршрута и проект процессуального документа |
| `ksrf-explore-arguments` | исследование конкурирующих гипотез | `ArgumentPortfolio` с основной, резервной и отклонёнными линиями |
| `ksrf-argument-patterns` | исследовательская библиотека практики КС РФ | candidate findings, аналоги, различия и refusal risks |
| `ksrf-echr-argumentation` | проверяемый сравнительный слой ЕСПЧ | supporting/adverse findings с пределами переноса |
| `ksrf-complaint-facts-demands` | факты, конституционный вопрос и требования | фактологическая трассировка и основная/узкая формулы remedy |
| `ksrf-rights-argument-builder` | правовая позиция | проверяемые разделы основного и резервного аргументов |
| `ksrf-complaint-qa` | независимая содержательная проверка | refusal model, список дефектов и решение о готовности |
| `ksrf-formal-filing-check` | техническая и формальная подача | комплектность, реквизиты, приложения и выбранный канал |
| `ksrf-decision-execution` | последствия акта КС РФ | план пересмотра, компенсации и нормативного исполнения |

## Dynamic. Типовой маршрут жалобы

```mermaid
C4Dynamic
title KSRF Skillset — динамика основного маршрута

Person(user, "Заявитель или юрист", "Подтверждает факты и принимает ключевые решения")
Container(agent, "Агентная среда", "Диалог и исполнение", "Управляет рабочей сессией")

Container_Boundary(skillset, "KSRF Skillset") {
  Component(cycle, "Полный цикл", "ksrf-complaint-cycle", "Оркестратор")
  Component(triage, "Первичная оценка", "ksrf-case-triage", "Hard gates и маршрут")
  Component(exhaustion, "Исчерпание", "ksrf-exhaustion-planner", "Стадии и срок")
  Component(explore, "Исследование", "ksrf-explore-arguments", "Портфель гипотез")
  Component(patterns, "Паттерны", "ksrf-argument-patterns", "Аналоги и контрпримеры")
  Component(echr, "ЕСПЧ", "ksrf-echr-argumentation", "Сравнительные findings")
  Component(facts, "Факты и требования", "ksrf-complaint-facts-demands", "Норма, применение, вред и remedy")
  Component(rights, "Правовая позиция", "ksrf-rights-argument-builder", "Основной и резервный аргументы")
  Component(qa, "Проверка качества", "ksrf-complaint-qa", "Модель отказа и дефекты")
  Component(formal, "Формальная проверка", "ksrf-formal-filing-check", "Готовность пакета")
  Component(execution, "Исполнение", "ksrf-decision-execution", "Действия после акта КС РФ")
}

Rel(user, agent, "Передаёт материалы и задачу")
Rel(agent, cycle, "Запускает цикл")
Rel(cycle, triage, "Проверяет hard gates и выбирает маршрут")
Rel(cycle, exhaustion, "Строит процессуальную линию")
Rel(cycle, explore, "Запускает конкурирующие гипотезы")
Rel(explore, patterns, "Проверяет аналоги, различия и риски отказа")
Rel(explore, echr, "Проверяет сравнительные аргументы")
Rel(cycle, user, "Показывает портфель гипотез для ручного выбора")
Rel(user, cycle, "Утверждает основную и резервную линии")
Rel(cycle, facts, "Собирает факты, вопрос и требования")
Rel(cycle, rights, "Собирает правовую позицию")
Rel(cycle, qa, "Передаёт проект на независимую проверку")
Rel(qa, cycle, "Возвращает дефекты или разрешает следующий этап")
Rel(cycle, formal, "Проверяет комплект перед подачей")
Rel(cycle, agent, "Возвращает готовый пакет и следующие действия")
Rel(cycle, execution, "После акта КС РФ строит план исполнения")
```

### Ответвления от основного маршрута

- если hard gate не пройден, цикл останавливается или возвращает список недостающих материалов;
- если обычное дело ещё рассматривается, может быть выбран `ksrf-court-request-motion` вместо индивидуальной жалобы;
- если исследование не подтверждает жизнеспособную позицию, проект не должен автоматически переходить к drafting;
- если QA находит некомпенсируемый дефект, формальная комплектация не делает жалобу готовой.

## Implementation. Устройство одного пакета скилла

```mermaid
C4Component
title KSRF Skill — внутренняя структура типового пакета

Container(agent, "Агентная среда", "Skill runtime", "Обнаруживает и исполняет скилл")
SystemDb_Ext(casefiles, "Материалы дела", "Документы и рабочие файлы")
SystemDb_Ext(official, "Официальные источники", "Актуальное право и практика")

Container_Boundary(skill, "skills/ksrf-*") {
  Component(metadata, "agents/openai.yaml", "YAML", "Название, описание и стартовая подсказка для обнаружения")
  Component(instructions, "SKILL.md", "Markdown", "Назначение, порядок работы, ограничения и контракт результата")
  Component(references, "references/", "Markdown + JSON", "Переносимая методология, карты, примеры и исследовательские корпуса")
  Component(scripts, "scripts/", "Python", "Детерминированные проверки, сбор и валидация; присутствуют не в каждом скилле")
}

Rel(agent, metadata, "Обнаруживает подходящий скилл")
Rel(agent, instructions, "Читает и выполняет рабочий процесс")
Rel(instructions, references, "Подключает нужные справочники")
Rel(instructions, scripts, "Запускает автоматизацию, когда она предусмотрена")
Rel(scripts, casefiles, "Читает и проверяет материалы")
Rel(instructions, official, "Требует актуальной проверки правовых источников")
Rel(references, official, "Сохраняет ориентиры, но не заменяет живую проверку")

UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

## Deployment. Источник правды и публикация

```mermaid
C4Deployment
title KSRF Skillset — рабочее размещение и публикация

Deployment_Node(github, "GitHub", "aegorfk/ksrf-skillset") {
  Container(repo, "Публичное зеркало skillset", "Git repository", "Версионируемые скиллы, документация, tools и OpenSpec")
}

Deployment_Node(workstation, "Рабочая машина", "Codex environment") {
  Container(global, "Глобальные KSRF-скиллы", "~/.codex/skills/ksrf-*", "Рабочий источник правды для исполнения и развития")
  Container(runtime, "Агентная среда", "Codex", "Загружает глобальные скиллы и ведёт рабочие сессии")
  Container(sync, "Синхронизация", "tools/sync_global_skills.sh", "Миррорит глобальные KSRF-скиллы в репозиторий")
}

Deployment_Node(matter, "Рабочая папка дела", "Локальные документы") {
  ContainerDb(files, "Материалы дела", "PDF / DOCX / TXT / Markdown", "Судебные акты, приложения и создаваемые артефакты")
}

SystemDb_Ext(official, "Официальные онлайн-источники", "КС РФ, законодательство и судебная практика")

Rel(runtime, global, "Обнаруживает и исполняет")
Rel(runtime, files, "Читает материалы и сохраняет результаты")
Rel(runtime, official, "Проверяет актуальные нормы и практику")
Rel(global, sync, "Передаёт актуальные пакеты")
Rel(sync, repo, "Публикует проверенную версию", "Git")
Rel(repo, global, "Устанавливается или обновляется на другой машине", "install.sh / Git")
```

## Архитектурные выводы

1. `ksrf-complaint-cycle` является единственной точкой оркестрации, но не концентрирует внутри себя всю методологию.
2. Исследование вынесено в отдельный контур `explore-arguments → argument-patterns / echr-argumentation`, чтобы drafting не фиксировал первую удобную гипотезу.
3. Между исследованием и составлением позиции предусмотрен ручной выбор основной и резервной линий.
4. `complaint-qa` и `formal-filing-check` решают разные задачи: первый проверяет юридическую устойчивость, второй — техническую готовность пакета.
5. `court-request-motion` образует альтернативный процессуальный маршрут, а `decision-execution` продлевает жизненный цикл за пределы подачи жалобы.
6. Встроенные references обеспечивают автономность, но актуальные нормы и факты конкретного дела всегда требуют проверки по первичным источникам.
