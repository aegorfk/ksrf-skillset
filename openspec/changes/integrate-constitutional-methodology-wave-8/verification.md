# Verification

## Source and attribution controls

- Four file-level records were checked with exact SHA-256, byte/page counts, bibliographic identity, author/editor roles and page locators. The ordered inbox-manifest receipt is `49e5b571c3177aeae90a46ca33ca9e44970c04146754a9643b7faad5a5499886`.
- Three files are new independent source families. The fourth, David L. Faigman's *Constitutional Fictions*, is an exact binary duplicate with SHA-256 `62346344eec01e5889822b66812003990223be7e10c1974d1effefb5e129b054`; it remains a separate receipt but does not increase evidentiary weight or the independent-family count.
- Representative pages were rendered and visually inspected: *European Yearbook of Constitutional Law 2021* PDF 4, 252 and 261; Faigman PDF 7, 105 and 123; *Cambridge Handbook of Psychology and Legal Decision-Making* PDF 5, 6, 220, 484 and 494; Lundmark chapter PDF 1 and 11.
- The private source map records promoted, overlap/no_delta and critic/eval_only dispositions. Three independent sources and the preserved duplicate were moved to explicit private archive destinations only after target preflight and post-move SHA verification; source files, OCR and reconstructive extracts are absent from this repository.

## Method and behavior controls

- Runtime additions are source-neutral: `ConstitutionalFactProofRoute`, `QuestionSubstitutionAndExposureCheck`, `ConstitutionalAdviceFunctionAndEffectCard` and `RiskCommunicationCard` contain triggers, near misses, adverse branches, stop rules and bounded outputs, but no book, author, local-path, PDF or network dependency.
- The application classifier now preserves proven court-authored implicit norm use when an independent sufficient ground blocks outcome causation. It returns `application_unclear` with separate implicit-use and independent-ground reasons instead of collapsing that record into `not_applied`.
- Independent forward review passed contracts A-E, source-independence/routing, strict OpenSpec and diff hygiene. The controlled-order check used separate reviewer contexts and a common input hash. A-D remain evaluation/reference contracts rather than executable classifiers, and their snapshot test asserts only bounded contract fields; E additionally exercises the application classifier. Same-context replay remains a diagnostic only, and the synthetic contracts do not establish legal validity on a real complaint.

## Automated verification on the pre-integration base

- Six affected skills passed the official `quick_validate.py` check.
- Targeted regression after the implicit-application correction: `64 passed, 257 subtests passed`.
- Full stable unittest discovery: `622 tests` passed in `661.836s`, with `11` expected skips and zero failures or errors.
- Clean-room installation into a separate target passed offline: `16/16` skills, `302` files, `11,035,980` bytes, zero errors and zero warnings; tree SHA-256 `5adbc4c4d26787742ec3aa1a95e08b1ca46e865a165ec7b1afa6248eb4acf788`.
- A diff-scoped source-identity, local-path, PDF, URL and network-dependency scan returned no runtime match. `git diff --check` passed.
- The stale digest assertion for the previously published case-triage official-retrieval route was corrected without changing the runtime file it describes.

The pre-integration base was `5f22ae1493a3cb38721885bbd873d42fc13c33ab`. Live remote inspection then found a newer independent commit `067909b161951f7d0d65f9cc432fc829d7d72300`, so the first candidate was not published. The change was rebased and reverified as recorded below. These checks validate bounded methodology and packaging only. They do not establish current Russian law, application to a particular applicant, admissibility, complaint readiness or human approval to file.

## Verification after integration with live `main`

- The substantive commit was rebased onto `067909b161951f7d0d65f9cc432fc829d7d72300`. The independent complaint-header QA paragraph from that base remains present; the only manifest conflict was resolved by regeneration rather than by choosing either stale side.
- The regenerated manifest is bound to base `067909b161951f7d0d65f9cc432fc829d7d72300`: `16` skills, `302` files, `11,040,080` bytes and runtime tree SHA-256 `7f81c8234f489b45786ebefa3e897d02cf139295c92ea9e1266716eaa2c4efc8`.
- Merged targeted regression passed: `64 passed, 257 subtests passed`. Merged full unittest discovery passed `622 tests` in `639.351s`, with `11` expected skips and zero failures or errors.
- All six changed skill entry points passed `quick_validate.py`. Strict source-profile validation then passed `16/16`, with zero errors and zero warnings after removal of test-generated bytecode only; the standalone offline self-containment verifier also passed `16/16`.
- A fresh merged clean-room installation passed offline with zero errors and zero warnings and reproduced the manifest tree digest. Strict change validation and `git diff --check` passed. A source-identity/network scan of the runtime diff against `067909b` returned no match.

Publication, remote-SHA confirmation, global synchronization and OpenSpec archive remain the only open release steps. The merged checks still prove bounded behavior and package integrity only, not legal validity on a real complaint.
