# enforce-runtime-command-path-resolution

Make every bundled command resolve from the actual installed skill root, make installation report that root safely, and require explicit configuration for the external HUDOC engine.
