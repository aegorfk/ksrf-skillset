## 1. Contract tests

- [x] 1.1 Add failing core tests for the exact retained triple, structurally valid `complete=false` compatibility output, every self/external/file/plan/ordered-candidate mismatch, partial input combinations, invalid contracts, and exact one-LF reliability bytes.
- [x] 1.2 Add failing schema tests for the closed report root, exact tri-state checks, fixed scope, closed reason/remediation enums, and rejection of extra or input-derived members.
- [x] 1.3 Add failing CLI tests for the nested route, optional diagnostic inputs, canonical deterministic stdout, empty handler stderr, and the exact `0`/`3`/`2` state priority.
- [x] 1.4 Add hostile-value, large-population availability, and side-effect regressions that snapshot inputs and directories; prohibit repeated candidate-list membership scans, file mutation, temporary files, subprocesses, network, database, repair, import, and bytecode writes.
- [x] 1.5 Add source/clean-install parity, ambient-`PYTHONPATH` isolation, exact-option, Russian-help inventory, and supported-Python regression cases.

## 2. Shared evaluator and report

- [x] 2.1 Extract one structured native-reliability evaluator in `practice_quality.py`, make the existing Release 17 verifier project from it, and prove its public success/failure behavior and downstream consumers remain unchanged.
- [x] 2.2 Implement the five-state doctor projection with the fixed precedence, closed reason order, exact Russian remediation mapping, negative authority scope, and no serialization of input-derived values.
- [x] 2.3 Add the closed `native_reliability_doctor_report` branch to `practice-quality.v1.json` additively without changing any existing artifact branch or `coding_reliability` v1.1.

## 3. Read-only CLI and guidance

- [x] 3.1 Register `quality native-reliability doctor`, its three exact long options and Russian metavars; read supplied inputs once under the strict canonical contracts; emit canonical JSON plus one LF; return the specified code; and expose no output, stdin, discovery, or repair path.
- [x] 3.2 Update installed `SKILL.md`, `references/practice-quality.md`, and CLI help with the copyable command, `0`/`3`/`2` meanings, Release 16 retained-anchor recovery, no-write/network/database boundary, and mandatory downstream current-plan revalidation.

## 4. Verification

- [x] 4.1 Run the focused judicial-meaning core/schema/CLI tests and root exit-status, Russian-help, option-exactness, launcher-isolation, and runtime-path tests until all are green.
- [x] 4.2 Run the full source and supported Python test matrices, strict skill/schema validators, privacy scans, install into a clean external target, and verify byte-identical source/installed reports with unchanged directory snapshots.
- [x] 4.3 Run `openspec status --change add-native-reliability-doctor` and `openspec validate add-native-reliability-doctor --strict --no-interactive`; retain the change unarchived until every implementation task and independent review is complete.

## 5. Release integration

- [x] 5.1 Regenerate `skills-manifest.json` and the release identity only after runtime verification, then prove the installed payload contains the changed library/schema/guidance and still excludes tests, evals, and OpenSpec.
- [x] 5.2 Synchronize the validated delta specs, archive the OpenSpec change, create the atomic release commit, publish, verify remote SHA equality, and install from that verified published checkout only after every prior task and independent review is complete.
