## 1. Source Control and Attribution

- [x] 1.1 Record the four received PDFs as four file-level records with exact hashes, bibliographic identities, page locators, author/editor roles, and the Faigman exact-duplicate relationship; report the independent source-family count separately.
- [x] 1.2 Classify every reviewed source or chapter as promoted, overlap/no_delta, critic/eval_only, or rejected, without making source files runtime dependencies.

## 2. Methodology Integration

- [x] 2.1 Extend the constitutional-fact ledger with stable route/channel-instance IDs, canonical Dependency-DAG references and separate dependency/support statuses; route shared primary sources to the existing dependency audit.
- [x] 2.2 Add a question-substitution and exposure-order check that permits `FrozenBaseline` only before exposure, labels an already exposed pass as `PostExposureReconstruction`, and treats only fresh isolated reviewers with the same input hash as a valid reverse-order comparison, while preserving separate direct and implicit application gates.
- [x] 2.3 Add a narrowly triggered constitutional-advice reference covering full-text and delivery evidence, function, type, mode, timing, uptake, authority, causal effect, and separate record/legal-use statuses.
- [x] 2.4 Add a narrowly triggered quantified-risk reference separating claim type, measurement, representation, threshold ownership, application, causation, reliance, and independent grounds while reusing canonical records.
- [x] 2.5 Preserve proven court-authored implicit norm use on its own axis when an independent sufficient ground blocks causal harm; do not collapse that combination into `not_applied`.
- [x] 2.6 Update only the relevant skill routers and keep the sixteen-package architecture and human filing gate unchanged.

## 3. Behavioral Evaluation

- [x] 3.1 Add trigger and near-miss evals for dependent fact channels, question substitution, advisory timing, and quantified-risk labels, including a negative already-exposed baseline case and a two-reviewer controlled-order case.
- [x] 3.2 Add adverse and changed-premise expectations that preserve unknowns, publication/delivery differences, independent grounds, direct/implicit application gaps and proven implicit norm use, and the distinction between same-context diagnostics and independent review.
- [x] 3.3 Extend structural tests for field presence, routing, source independence, exact deduplication, role-accurate attribution, and package-count preservation.

## 4. Verification and Publication

- [x] 4.1 Render and visually inspect representative pages from every received PDF, including title/author and promoted-method locators.
- [x] 4.2 Run targeted tests, the full test suite, strict OpenSpec validation, and a scan proving that runtime skills contain no new source identities or network dependency; repair the stale exact-digest metadata for the previously published case-triage official-retrieval route without changing that runtime file.
- [x] 4.3 Run an independent forward review against the new behavioral cases; for the controlled-order case use fresh isolated reviewer contexts bound to the same input hash, and document that same-context replay is non-independent and that the result proves only bounded synthetic behavior.
- [x] 4.4 Complete tasks and verification records, sync the capability spec, and archive the OpenSpec change.
- [x] 4.5 Commit the exact allowlisted change, publish it to `aegorfk/ksrf-skillset:main`, verify the remote SHA, and synchronize the canonical global skills from the published tree.

## 5. Private Source Archive

- [x] 5.1 Update the project-side wave-eight source map and archive index without committing or publishing private source content.
- [x] 5.2 Move the three independent sources and the preserved exact duplicate from `ТЗ/Гайды/Новое` to explicit private archive destinations after extraction and verification.
