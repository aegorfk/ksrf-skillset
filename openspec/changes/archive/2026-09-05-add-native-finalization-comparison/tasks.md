## 1. Contract tests

- [x] 1.1 Add failing core tests for distinct direct siblings, safe common-parent
  capture, exact four-file inventories, `0700`/`0600`, owner/link/ACL rules, every
  per-file bound, uncertain internal relation, repeated native relation, required
  external repeat digest, and raw four-file equality.
- [x] 1.2 Add failing report-schema tests for the exact root, four statuses, twenty
  tri-state checks, ordered closed reasons, exact fixed remediation, fixed negative
  scope, and rejection of every extra or input-derived value.
- [x] 1.3 Add failing CLI tests for the nested route, three required exact long options,
  Russian metavars/help, canonical stdout, empty handler stderr, parser boundary, and
  exact `0`/`3`/`2` precedence.
- [x] 1.4 Add hostile-value and TOCTOU tests for leaf/parent replacement, same directory,
  cross-parent inputs, symlink/FIFO/device/subdirectory/extra entries, hardlinks and
  cross-directory inode aliasing, mode/owner/ACL drift, same-inode rewrite, final
  recapture changes, close uncertainty, and stdout interruption.
- [x] 1.5 Add availability and side-effect tests for per-file/list bounds, linear
  validation, chunked full comparison, unchanged bytes/inventories, bytecode
  suppression, and prohibitions on mutation, temporary files, subprocesses, network,
  database, finalization, repair, quarantine, import, attachment, and promotion.

## 2. Safe capture and comparison evaluator

- [x] 2.1 Implement one descriptor-held finalization-directory capture that reuses the
  established safe-parent, no-follow, private-mode, effective-owner, Darwin ACL,
  stable-identity, bounded-read, and exact-inventory primitives.
- [x] 2.2 Validate strict canonical receipt/reliability contracts, each unsigned-object
  receipt self-digest, all three receipt-to-sibling file digests, the uncertain internal
  relation, and the repeated externally anchored native relation by reusing the shared
  evaluator; compare all four corresponding raw file byte streams without inventing a
  full receipt-file digest.
- [x] 2.3 Recapture both directories completely through the original held parent before
  report delivery and fail closed on every identity, metadata, ACL, inventory, byte,
  parent-path, leaf-name, descriptor-close, or capability uncertainty.
- [x] 2.4 Build the closed report projection with exact state/reason/check/remediation
  order, tri-state prerequisites, negative scope, and no serialization of paths,
  digests, identifiers, counts, contents, coordinates, exceptions, or environment.

## 3. Installed CLI schema and guidance

- [x] 3.1 Register `quality native-reliability compare-finalizations`, all three required
  options and Russian metavars, canonical one-line report output, and the exact process
  mapping without changing `doctor`, `coding-audit-finalize`, or downstream commands.
- [x] 3.2 Add `native_finalization_comparison_report` to the installed
  practice-quality schema additively and validate exact enums, tri-state checks,
  remediation objects, fixed scope, and `recovery_comparison_valid` equivalence.
- [x] 3.3 Update installed `SKILL.md`, `references/practice-quality.md`, and CLI help
  with the copyable route, external-anchor origin, eligible-vs-administrator recovery
  boundary, `0`/`3`/`2` meanings, no-side-effect boundary, and downstream legal gates.

## 4. Verification

- [x] 4.1 Run focused judicial-meaning core/schema/CLI tests plus existing finalizer,
  doctor, downstream binding, exit-status, Russian-help, option-exactness, and
  runtime-path regressions.
- [x] 4.2 Run full source and supported-Python suites, strict skill/schema/privacy
  validators, and clean external installation; prove byte-identical source/installed
  reports for all four states with unchanged directory snapshots and conflicting
  ambient `PYTHONPATH`.
- [x] 4.3 Run `openspec status --change add-native-finalization-comparison` and
  `openspec validate add-native-finalization-comparison --strict --no-interactive`;
  retain the change unarchived until every implementation task and independent review
  is complete.

## 5. Release integration

- [x] 5.1 Refresh `skills-manifest.json` and release identity only after runtime
  verification; prove the installed payload carries modified runtime/schema/guidance
  and still excludes tests, evals, and OpenSpec.
- [x] 5.2 Synchronize validated delta specs, archive the OpenSpec change, create one
  atomic release commit, publish it, verify remote SHA equality, and install only from
  that verified checkout after all prior tasks and independent review pass.
