## 1. Contract and Red Tests

- [x] 1.1 Validate the runtime-only boundary and exact marker scope.
- [x] 1.2 Add a failing exact-payload test that identifies the three current maintainer traces.
- [x] 1.3 Add failing validator tests for the stable finding, bounded evidence, source-only exclusion, and legal-numbering negative controls.

## 2. Runtime Cleanup and Guard

- [x] 2.1 Replace the three internal workflow references without removing any substantive safety or human-approval gate.
- [x] 2.2 Add the normalized maintainer-workflow classifier and fail-closed runtime finding.
- [x] 2.3 Preserve source profile behavior, runtime file count, public report shape, and all existing validation gates.

## 3. Verification and Review

- [x] 3.1 Prove red-to-green transition and exact zero-offender runtime payload.
- [x] 3.2 Run focused, root, skill, strict source/runtime, clean-room, shell, manifest, self-containment, and quick skill checks.
- [x] 3.3 Run strict OpenSpec validation and independent spec/security review; resolve all P1/P2 findings.

## 4. Atomic Publication and Installation

- [x] 4.1 Archive the validated change and synchronize the base spec.
- [x] 4.2 Regenerate the manifest against exact current `main` and create one atomic release commit.
- [x] 4.3 Push the feature ref and fast-forward `main` to the identical SHA; confirm publication state.
- [x] 4.4 Install the exact published runtime globally and verify offline/current behavior.
