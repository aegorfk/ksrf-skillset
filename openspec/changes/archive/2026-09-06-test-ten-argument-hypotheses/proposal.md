## Why

The user requests actual testing of ten legal-argument hypotheses. H1–H5 exist as research methods but comparative effect is unmeasured. H6–H10 must first be registered. Human legal evaluation remains separate; it does not prevent a bounded model development pilot now.

## What Changes

- Register ten matched source-bound probes, including five narrow extensions of existing methods.
- Add a maintainer-only isolated Codex runner, concealed-variant model review and DeepEval diagnostics with authenticated Langfuse trace readback.
- Run the pilot on real, locally held primary documents; retain source packets and reconstructive outputs outside Git.
- Publish only the protocol, tools, non-reconstructive aggregate findings and any independently justified method correction. Null or adverse results are retained.
- Restore two inherited source-credit regression checks after the already published table-numbering change, preserving their attribution, link, column and adjacency assertions without changing source content or experiment inputs.

## Capabilities

### New Capabilities
- `ksrf-ten-method-development-probes`: retrospective, source-bound model development comparisons that cannot certify legal quality or filing readiness.

## Impact

Maintainer tools/tests and OpenSpec only by default. No production prompt, provider change, native RSI unpause, human-review substitution, corpus eligibility change or document publication. No runtime method expansion unless the observed failure warrants it.
