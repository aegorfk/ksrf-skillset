## 1. Blinded packet contract tests

- [x] 1.1 Add closed-schema tests for reviewer material and inner manifest rows,
  plus distinct legacy five-file and versioned `1.1` six-file parent-manifest
  branches; reject mixed or mis-versioned shapes.
- [x] 1.2 Add source/install parity and deterministic ZIP metadata/content tests.
- [x] 1.3 Prove the inner ZIP stays byte-identical when valid primary substantive
  answers, first-coder identity, and per-lane membership change without changing
  the selected union, independently selected codebook version, neutral frozen-plan
  projection, or source inputs; unchanged judicial text may contain a primary quote
  without invalidating this structural test.
- [x] 1.4 Test exact supplied-text digest, pending templates, safety flags, and
  parent-manifest binding, including equality with the externally reported ZIP
  SHA-256.
- [x] 1.5 Add multi-candidate omission, duplicate, extra-ID, swapped-identity, and
  mismatched-text tests proving exact one-to-one parity among required IDs,
  materials, templates, and inner-manifest population.
- [x] 1.6 Prove the clean-installed producer works with installed `tests/` and
  `evals/` absent and does not resolve any runtime fixture from those source-only
  trees.
- [x] 1.7 Test that required `--codebook-version` accepts only `1.0`, resolves the
  tracked neutral codebook independently of primary answers, rejects every primary
  mismatch, and fails closed for a missing, unsafe, empty, non-UTF-8, or concurrently
  replaced codebook.
- [x] 1.8 Test the exact closed neutral-brief projection, its self/file/manifest hash
  bindings, absence of query/match/lane/primary/`approved_by`/`adverse_review`
  metadata, exactly-one-`hypothesis_under_test` rule, and no-output failure for an
  open, zero, or multiple question set.
- [x] 1.9 Test the exact captured-text policy: TAB, LF, VT, FF, and CR remain
  byte-exact, while NUL, every other `Cc`, and every `Cf` or `Cs` code point prevents
  publication; duplicate source copies with different exact layout also fail rather
  than selecting by source ID, and runtime output remains valid against the
  annotated schema contract.

## 2. Runtime production

- [x] 2.1 Extend native audit projection with selected full-text review materials
  and separate normalized-store versus exact-packet text digests, omitting
  `source_id` and `source_ids` from the reviewer payload.
- [x] 2.2 Add deterministic stored-ZIP assembly with fixed safe entry metadata.
- [x] 2.3 Add the self-digesting inner manifest and the ZIP as the sixth exact
  content file of an atomic `bundle_contract_version="1.1"` parent bundle while
  preserving the exact legacy five-file schema branch.
- [x] 2.4 Emit `independent_review_packet_sha256` in successful stdout with exact
  parity to the parent manifest's ZIP digest.
- [x] 2.5 Preserve existing sampling, manual expert, and reliability contracts
  without adding network, review-decision import, or adjudication.
- [x] 2.6 Require allowlisted `--codebook-version 1.0`, load the tracked neutral
  codebook, use every primary version only as an equality check, and bind the exact
  codebook bytes in both manifests.
- [x] 2.7 Produce a closed self-digesting neutral brief from exactly one directional
  `hypothesis_under_test` and reviewer-needed frozen-plan fields, bind its exact
  bytes in both manifests, and fail without output for an open, zero, or
  multi-question plan.

## 3. Installed user guidance

- [x] 3.1 Update Russian CLI help to identify the one file to share and warn
  against sharing the parent custodian directory; explain external ZIP-digest
  retention and comparison without overstating authentication.
- [x] 3.2 Document blinding scope, full-text/privacy boundary, reviewer steps, and
  pending human/legal gates in installed references and README.
- [x] 3.3 Make the embedded guide enumerate the exact closed 20-field record,
  labels, relations, substantive-speaker rule, nested alternative-ground shape,
  strict JSONL constraints, and absence of a native decision import/receipt.
- [x] 3.4 Document the required built-in codebook selection, six-member ZIP,
  neutral-brief scope, one-hypothesis limitation, exact text-control policy, and both
  inner/parent codebook and brief hash bindings.

## 4. Verification and release

- [x] 4.1 Run focused native-producer, schema, CLI, determinism, and adversarial tests.
- [x] 4.2 Run full skill/repository suites, strict source validation, manifest,
  clean-install verification, and offline self-containment checks.
- [x] 4.3 Obtain independent review with no unresolved P1/P2, complete tasks,
  validate/archive OpenSpec, and revalidate archived specs.
- [x] 4.4 Commit atomically, push feature and exact commit to main, verify remote
  SHAs, and install that exact published tree globally.
