## 1. Contract and Red Regression

- [x] 1.1 Record the bootstrap-only boundary and distinguish local setup help
  from authoritative configured-engine help.
- [x] 1.2 Add literal RED output coverage for both resolvers and both exact help
  flags in a fully unconfigured environment.
- [x] 1.3 Prove bootstrap help reaches no candidate discovery, filesystem/Git
  search, version inspection, or process execution.
- [x] 1.4 Preserve RED/green coverage for unconfigured non-help, combined help,
  present-empty configuration, incompatible versions, and configured argv
  forwarding.

## 2. Minimal Runtime Fix

- [x] 2.1 Recognize exact local help before discovery only when both relevant
  configuration keys are absent.
- [x] 2.2 Provide resolver-specific Russian setup guidance and route actual CLI
  help to the engine after explicit configuration.
- [x] 2.3 Preserve every non-bootstrap discovery, version, exclusivity,
  environment, exec, exit, and legal-readiness contract.

## 3. Verification and Review

- [x] 3.1 Prove the focused RED-to-GREEN transition and run existing resolver,
  runtime-help, command-path, and installed-copy contracts.
- [x] 3.2 Run full root and skill suites, strict source/runtime validation,
  offline containment, quick skill checks, manifest, and clean-install checks.
- [x] 3.3 Obtain independent implementation, adversarial, and trust-boundary
  review; resolve every in-scope P1/P2.

## 4. Atomic Publication and Installation

- [x] 4.1 Archive the validated change and synchronize its base capability.
- [x] 4.2 Regenerate the manifest and create one atomic release commit.
- [x] 4.3 Push the feature ref and `main` to the identical commit and confirm
  the live SHA.
- [x] 4.4 Install the exact published runtime globally and verify its identity.
