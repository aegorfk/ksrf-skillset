## 1. Contract and RED baseline

- [x] 1.1 Freeze live SHA, runtime/release manifests, twelve-gate prose contract, five route decisions and the absent runtime/schema surface.
- [x] 1.2 Record assumptions, proposal, design, delta requirements, command/test plan, boundaries and rollback before runtime implementation.
- [x] 1.3 Strictly validate the OpenSpec change.
- [x] 1.4 Add failing schema, domain, workflow/CLI, cross-skill and clean-install tests.

## 2. Versioned admissibility artifacts

- [x] 2.1 Add Draft 2020-12 schemas for `AdmissibilityMatrix` and `KSRFRouteRecommendation` with exact gates and conditional invariants.
- [x] 2.2 Add dependency-free matrix normalization and validation with exact Russian field/gate errors.
- [x] 2.3 Add deterministic route derivation, complete blocker reporting and immutable human/filing boundaries.

## 3. Installed route and guidance

- [x] 3.1 Add `admissibility validate|derive|status` to the workflow router and Russian CLI without network/model calls or workspace migration.
- [x] 3.2 Resolve claimed current official evidence and issue-option bindings through the existing local repositories before GO is eligible.
- [x] 3.3 Link the executable route and schemas from complaint cycle, case triage, argument exploration, complaint QA and the shared artifact contract.

## 4. Verification and publication

- [x] 4.1 Regenerate the manifest and run focused/full suites, source strict, clean-room runtime/offline, OpenSpec strict and diff checks.
- [x] 4.2 Obtain independent legal-contract, runtime and release reviews with no unresolved P1/P2.
- [ ] 4.3 Publish atomically to `main`, confirm live SHA, install and verify the exact global payload.
- [ ] 4.4 Archive the OpenSpec change, regenerate from the merge SHA and publish the final evidence commit.
